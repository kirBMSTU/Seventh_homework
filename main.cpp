#include "HW7.hpp"

int main()
{
  Queue<int> alpha = Queue<int>();
  //Queue<int> beta = Queue<int>(42);

  // alpha.Enqueue(2);
  // alpha.Enqueue(5);
  // alpha.Enqueue(7);
  //
  // beta.Enqueue(15);
  // beta.Enqueue(21);
  // beta.Enqueue(54);
  // beta.Enqueue(11);

  //Queue<int> teta = Queue<int>(beta);

  // std::cout << alpha;
  // std::cout << beta;
  // std::cout << teta;
  return 0;
}
